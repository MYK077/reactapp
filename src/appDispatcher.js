import { Dispatcher } from "flux";
const dispatcher = new Dispatcher();

export default dispatcher;

//  there is a single dispatcher per application
// the dispatcher is central hub for the application
